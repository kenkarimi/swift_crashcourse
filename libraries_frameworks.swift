import Foundation
import StoreKit
import UIKit

let button = UIButton();

/*This file doesn't compile because StoreKit & UIKit
  aren't really parts SwiftForWindows package we're using to learn Swift
  on a windows platform.
*/

